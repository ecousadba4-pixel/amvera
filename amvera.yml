name: u4s-loyalty
runtime: nodejs
version: 18

build:
  commands:
    - npm ci --only=production

run:
  command: node server.js  # Явно указываем Node.js

endpoints:
  www:
    http:
      port: 3000
